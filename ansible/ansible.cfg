[defaults]
inventory = inventory.ini

# Path to private key file for SSH authentication
private_key_file = ~/.ssh/vpc-connection-key.pem

# Default remote user for all hosts
remote_user = ubuntu

# Ensures password prompting if necessary (good for shared private keys)
ask_pass = True

# Enable parallel execution for better performance with large inventories
forks = 10